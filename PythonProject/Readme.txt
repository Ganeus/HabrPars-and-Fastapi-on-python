Порядок команд для запуска (если все установленно)( это нужно из за того что БД не успевает создастся к
моменту начала работы api )
docker compose up --build
docker compose up

Полезные команды (чтоб ручками не писать):
docker compose up --build
docker compose up
pip install
docker compose down -v  Чтоб снести БД

#Нужно написать парсер статей с сайта habr.ru.

#Также плюсом должно быть веб-приложение для работы с этими статьями (fastapi), с роутами:
#- /api/articeles - получение статей, с возможностью поиска по тексту, содержимому ✓
#- /api/articles/{article_id:int} - получение конкретной статьи ✓
#- /api/export/articles - экспорт статей в csv формат ✓

#Технологии (обязательные):
#- Docker/Docker Compose ✓
#- PostgreSQL ✓
#- SQLAlchemy ✓
#- FastAPI  ✓
#- BeautifulSoup4 ✓
#- и другие дополнительные на выбор, если потребуются ✓

#Парсим:
#1) Ссылка на изображение ✓
#2) Заголовок ✓
#3) Текст статьи ✓
#4) Количество комментариев ✓
#5) Количество голосов ✓
#6) Количество закрепов ✓



